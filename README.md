# 旅游网站 - 后端

Spring Boot 3.5.16 + MyBatis-Plus + Redis + MySQL

## 快速启动

```bash
# 1. 确保 MySQL 已创建 travel 数据库
# 2. 导入初始数据（在数据库/目录下）
# 3. 修改 application.yml 中的数据库/Redis 连接配置
# 4. 启动
mvn spring-boot:run
```

访问 API 文档：http://localhost:8080/doc.html

默认管理员：admin / 123456

详细说明请参见根目录 README。

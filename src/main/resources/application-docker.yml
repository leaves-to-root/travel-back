# ============================================
# Docker 容器环境配置
# 激活方式：java -jar app.jar --spring.profiles.active=docker
# ============================================
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/travel?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  data:
    redis:
      host: redis
      port: 6379
      password: 123456

server:
  port: 8080

spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456
      database: 0
      lettuce:
        shutdown-timeout: 10000
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 100
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/travel
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB
      enabled: true

mybatis-plus:
  mapper-locations: "classpath*:/mapper/**/*.xml" # Mapper.xml??????????????
  type-aliases-package: com.onlinejudge.pojo.entity
  global-config:
    db-config:
      id-type: auto # ??id??????
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
